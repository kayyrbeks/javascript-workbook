import computeRoomArea from "./computeRoomArea.js";

const roomWidth = Number(prompt("Enter the width of a room:"));
const roomLength = Number(prompt("Enter the length of a romm:"));

console.log(computeRoomArea(roomWidth, roomLength));
