import computeRoomArea from "./computeRoomArea.js";

const roomWidth = prompt("Enter the width of a room:");
const roomLength = prompt("Enter the length of a romm:");

console.log(computeRoomArea(roomWidth, roomLength));
